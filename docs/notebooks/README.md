# Documentation notebooks

This directory holds user-facing narrative notebooks. They should read generated files from
`data/profiles/`, `data/radii/`, and `data/qa/` rather than run SCF calculations.

Current notebook:

```text
proatomic_profiles_v1.ipynb
```

The notebook is the active report/story layer for the current repository state. Production
artifacts are generated by scripts and stored under `data/`.
