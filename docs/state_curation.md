# State curation

Curated generator states are stored in `data/states/curated/atom_states_v1.json`.
The table is generated from compact source tables and the `required_states_v1.csv`
selection file. Per-record `source` / `sources` fields are intentionally not part of the
state schema; attribution is documented at the source-table and README level.
